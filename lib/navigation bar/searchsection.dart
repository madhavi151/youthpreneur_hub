import 'package:flutter/material.dart';

class SearchSectionPage extends StatelessWidget {
  const SearchSectionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Search Section'));
  }
}
