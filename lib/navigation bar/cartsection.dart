import 'package:flutter/material.dart';

class CartSectionPage extends StatelessWidget {
  const CartSectionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Cart Section'));
  }
}
